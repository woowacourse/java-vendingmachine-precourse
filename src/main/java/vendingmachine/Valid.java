package vendingmachine;

public class Valid {
    private static boolean isValidMoney(String[] productList) {
        for ()
    }

    public static boolean isValidProductInformation(String userInput) {
        String[] productList = userInput.split(";");
        return isValidMoney;
    }
}
